/** Free live-slot replacements allowed per calendar month (UTC). */
export const FREE_SLOT_REPLACEMENTS_PER_MONTH = 2;

/** Minimum strict, live comparables before returning any pricing guidance. */
export const PRICE_GUIDE_MIN_COMPS = 10;

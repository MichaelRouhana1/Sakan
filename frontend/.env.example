# Clerk Authentication (OAuth social login)
# Without this key, app still boots — email/password only. OAuth buttons hidden.
# EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# Optional override. On Expo Go + physical device, api.ts auto-uses your Metro LAN IP.
# Set this if auto-detect fails or you use a remote API.
# EXPO_PUBLIC_API_URL=http://192.168.x.x:3001
#
# Email/password registration uses the backend API (see backend/.env.example).

# Maps: Expo Go uses Apple Maps (iOS) / Expo's Google key (Android).
# For a custom Android build, set a Google Maps API key in app.json:
#   android.config.googleMaps.apiKey
# Optional: EXPO_PUBLIC_GOOGLE_MAPS_API_KEY if you wire it into app.config.js later.
